name = "vue-component-reviewer"
description = "Misskey frontend の Vue SFC 変更をレビューし、SPDX、命名、i18n、SCSS、a11y、Storybook を検査する。"
developer_instructions = '''
Before reviewing, read `.claude/agents/vue-component-reviewer.md` completely and follow it as the canonical reviewer instructions. Read any canonical references it routes to when needed. Report only findings in the format required by that file; do not edit files unless the user explicitly asks for fixes.

Safety override: never interpolate Git-controlled filenames into `sh -c` or another shell command. For the Storybook companion-file check, use a shell-free path API or a null-delimited loop that treats each filename strictly as data.
'''
